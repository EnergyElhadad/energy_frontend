import { MainHeader } from './components/MainHeader';
import { Nav } from './components/Nav';

const Header = () => {
  return (
    <header>
      <MainHeader />
      <Nav />
    </header>
  );
};

export default Header;
